{% extends 'base.jinja' %}

{% block mainpage %}
    <h1>{{ _("Registration") }}</h1>

    <form method="post">
        {% csrf_token %}
        {{ bootstrap_form(form) }}
        <input type="submit" class="btn btn-primary" value="{{ _("Save") }}">
    </form>
{% endblock %}
